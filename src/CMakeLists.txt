include_directories( ${OpenCV_INCLUDE_DIRS} )
include_directories( ../include )
set(SOURCES videoparameters.cpp main.cpp)
add_executable( test ${SOURCES} .. )
target_link_libraries( test ${OpenCV_LIBS} )
